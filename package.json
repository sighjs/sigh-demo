{
  "name": "sigh-tst",
  "version": "1.0.0",
  "description": "Sigh test",
  "main": "index.js",
  "scripts": {
    "test": "rm -rf dist && sigh && ! node index"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sigh-traceur": "^0.1.0"
  },
  "dependencies": {
    "source-map-support": "^0.2.8"
  }
}
